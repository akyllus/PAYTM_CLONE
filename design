import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Paytm Clone',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: PaytmHomePage(),
    );
  }
}

class PaytmHomePage extends StatefulWidget {
  @override
  _PaytmHomePageState createState() => _PaytmHomePageState();
}

class _PaytmHomePageState extends State<PaytmHomePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Paytm Clone'),
      ),
      body: Column(
        children: [
          Expanded(
            child: ListView(
              children: [
                ListTile(
                  leading: Icon(Icons.account_balance_wallet),
                  title: Text('Wallet'),
                ),
                ListTile(
                  leading: Icon(Icons.credit_card),
                  title: Text('Payments'),
                ),
                ListTile(
                  leading: Icon(Icons.shopping_cart),
                  title: Text('Shop Now'),
                ),
                ListTile(
                  leading: Icon(Icons.card_giftcard),
                  title: Text('Offers'),
                ),
                ListTile(
                  leading: Icon(Icons.settings),
                  title: Text('Settings'),
                ),
              ],
            ),
          ),
          Container(
            padding: EdgeInsets.all(10),
            child: RaisedButton(
              onPressed: () {
                // Add your logic here
              },
              child: Text('Pay Now'),
            ),
          ),
        ],
      ),
    );
  }
}
